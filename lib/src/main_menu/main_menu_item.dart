import 'package:flutter/material.dart';

/// A placeholder class that represents an entity or model.
class MainMenuItem {
  const MainMenuItem(this.text, this.icon);

  final String text;

  final Icon icon;
}
